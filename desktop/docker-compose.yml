version: "3.3"

services:
  desktop:
    build: .
    ports:
         - '8080:8080'
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules

networks:
  default:
    external:
      name: server
